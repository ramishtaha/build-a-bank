# ── Secrets (NEVER commit real values; copy .env.example → .env locally) ──────
.env
*.local
*.pem
*.key
*-secret*.yaml
!*.example

# ── Java / Maven ──────────────────────────────────────────────────────────────
target/
!.mvn/wrapper/maven-wrapper.jar
*.class
*.jar
*.war
!**/maven-wrapper.jar
hs_err_pid*
replay_pid*
*.jfr

# ── Node / Frontend (Phase F) ─────────────────────────────────────────────────
node_modules/
dist/
.vite/
*.tsbuildinfo
coverage/
playwright-report/
test-results/

# ── Python / ML (Phase I) ─────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
mlruns/
*.onnx

# ── IDE / editor (we DO commit .run/ run-configs on purpose) ──────────────────
.idea/
!.run/
*.iml
.vscode/
.settings/
.classpath
.project

# ── OS noise ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs / scratch ────────────────────────────────────────────────────────────
*.log
logs/
tmp/

# jqwik failure-sample database (property-based testing, Step 28) — local, not committed
.jqwik-database

# overnight-loop halt signal — created by the operator during a run, never committed
STOP
