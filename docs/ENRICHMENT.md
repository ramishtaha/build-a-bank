# 📚 Lesson Enrichment Tracker

A documentation-only pass that brings each step's `lesson.md` up to the full **MASTER_PROMPT §8.1** Hands-On Build Playbook bar (the DEPTH GATE in §8): every build sub-step carries the complete micro-anatomy — 🎯 Goal → 📁 Path → ⌨️ complete verbatim code → 🔍 line-by-line → 💭 under-the-hood → 🔮 predict → ▶️ Run & See (fenced ✅ expected output) → ✋ checkpoint → 💾 commit → ⚠️ pitfall.

**Code is FROZEN.** This pass never modifies code, tests, poms, configs, scripts, or git tags — only `steps/step-NN/*` lesson aids and files under `docs/`. Verified facts (Verification Logs, recorded run numbers) are preserved.

**Enrichment range: steps 11 → 30 only.** One step per session.

| Step | Status | Notes |
|---|---|---|
| 11 | ✅ enriched | Concurrency lab. Build expanded to 10 full-micro-anatomy sub-steps with verbatim `step-11-end` code (pom, `Balance` + 4 impls, both test classes, smoke.sh). No `requests.http`/seed — the module is pure-JVM JUnit with **no endpoints/services**. Not a multiple of 5 → no Cumulative Review. Flashcards already present in `docs/flashcards.md`. Interview Q&As + Key Terms appended to `docs/interview-bank.md` / `docs/glossary.md`. Verification Log preserved intact. |
| 12 | ✅ enriched | Phase-B finale: the `demand-account` service. Build rewritten to **12 full-micro-anatomy sub-steps** (module wiring + root-pom diff, config + compose, Flyway V1, `Account`/`EntryDirection`/`InsufficientFundsException`, `LedgerEntry`/`AuditEntry`, 3 repositories incl. `FOR UPDATE` + unsafe bulk update, `TransferService` safe+unsafe, `AuditService`/`PropagationDemoService` REQUIRES_NEW, web layer + main class, `ContainersConfig`+`TransferServiceTest`, the capstone `ConcurrentTransferTest`, then optimistic/propagation/web-slice/live-HTTP tests) — **all code pasted verbatim from `step-12-end`** (later steps may refactor; the tag is the truth). 2,622 lines / 148 fenced blocks. `requests.http` already present (endpoints exposed) — not re-created. `docs/flashcards.md` already had Step 12 — untouched. Interview Q&As (7) + Key Terms appended to `interview-bank.md` / `glossary.md`. Verification Log preserved intact (11 tests, capstone both ways, §12.3 mutation 17/20, clean-room). Not a multiple of 5 → existing Phase-B Cumulative Review (Steps 8–12) retained and lightly extended (8 questions). Nothing unrecoverable. |
| 13 | ✅ enriched | Phase-C opener: the demand-account **web layer** (Spring MVC deep). Build rewritten to **8 full-micro-anatomy sub-steps** (0 springdoc pom diff · 1 `GlobalExceptionHandler` domain handlers, replacing the deleted `ApiExceptionHandler` placeholder shown verbatim · 2 validation override + **whole file** · 3 `OpenApiConfig`/Swagger UI · 4 `RequestIdFilter` · 5 `TimingInterceptor`+`WebConfig` · 6 `TransferControllerTest` slice diff+whole-file · 7 `DemandAccountIntegrationTest` live-HTTP diff+whole-file + `verify`) — **all code pasted verbatim from `step-13-end`** (HEAD has since refactored the web pkg heavily — payments/webhooks/security; the tag is the truth). Added the existing **unchanged web layer** (`TransferController` + 4 DTOs) verbatim to Your Starting Point for context. **1,636 lines / 108 fenced-delimiter lines** (was 812/58). `requests.http` already present (endpoints exposed; seeds via `POST /api/accounts`) — not re-created; no separate seed file needed. `docs/flashcards.md` already had Step 13 (also 14/15) — untouched. Interview Q&As (6 + STAR seed) + Key Terms (24) appended to `interview-bank.md` / `glossary.md`. Verification Log preserved intact (13 tests, live OpenAPI/Swagger/Problem+JSON, §12.3 mutation 422→200, clean-room). Not a multiple of 5 → no Cumulative Review. Nothing unrecoverable. |
| 14 | ✅ enriched | Phase-C: API design — URI versioning, idempotency, pagination & signed webhooks. Build rewritten to **9 full-micro-anatomy sub-steps** (0 V2 migration + `IdempotencyRecord` entity + repository · 1 `IdempotentTransferService` lookup-or-execute · 2 pagination plumbing — `LedgerEntryRepository` paged-finder diff + `TransferService.entriesOf` diff · 3 `PageResponse` + `LedgerEntryResponse` envelopes · 4 `WebhookSigner` HMAC+replay-window + `WebhookSignerTest` · 5 `WebhookSender` bounded retries · 6 `WebhookPublisher` config-gated · 7 `TransferController` /api/v1 + deprecation — diff **and** whole file · 8 tests `IdempotencyTest` whole-file + `TransferControllerTest`/`DemandAccountIntegrationTest` diffs + ADR-0006) — **all code pasted verbatim from `step-14-end`** (HEAD has since refactored the web/webhook pkg — payments/security; the tag is the truth). Added the unchanged `TransferRequest`/`TransferResponse` DTOs to Your Starting Point for context. **1,771 lines / 108 fenced-delimiter lines** (was 741/46). Ran the **pure-JUnit** `WebhookSignerTest`+`WebhookDeliveryTest` live (no Docker) → real fresh output (6 tests, random ports 58129/58131, genuine 500→retry→200) folded into Run-and-See §4-5 and Verification Log §3. `requests.http` already present (endpoints exposed; seeds via `POST /api/accounts`) — not re-created. `docs/flashcards.md` already had Step 14 — untouched. Interview Q&As (7 + STAR seed) + Key Terms (23) appended to `interview-bank.md` / `glossary.md`. Verification Log preserved intact (25 tests, idempotent retry, signed delivery+replay, §12.3 mutation, clean-room). Not a multiple of 5 → no Cumulative Review. Nothing unrecoverable. |
| 15 | ✅ enriched | Phase-C: API Gateway / BFF + service-to-service HTTP. Build rewritten to **10 full-micro-anatomy sub-steps** (0 gateway module pom + root-pom diff · 1 `GatewayApplication` · 2 `application.yml` routes (StripPrefix + AddResponseHeader, server-webmvc prefix) · 3 `GatewayRoutingTest` whole-file (in-test `HttpServer` stub + `@DynamicPropertySource`) · 4 `CifCustomer` DTO · 5 `CifClient` `@HttpExchange` interface · 6 `CifClientFactory` (RestClient + connect/read timeouts) · 7 `CifClientConfig` bean · 8 `CifClientTest` whole-file (deserialize + fail-fast) · 9 play/verify harness — smoke.sh + requests.http + Makefile diff + ADR-0007) — **all code pasted verbatim from `step-15-end`** (HEAD has since grown the gateway — CORS config + more routing tests; the tag is the truth). **1,411 lines / 102 fenced-delimiter lines** (was 684/48). Ran the **pure-JUnit, unchanged-since-tag** `CifClientTest` live (no Docker) → real fresh output (2 tests, 2.521s, genuine 400ms-timeout→`ResourceAccessException`) folded into Run-and-See §8 and Verification Log §2. Gateway test count (`Tests run: 1`) preserved from the step-15-end Verification Log (the class grew in later steps) — flagged §12.8. `requests.http`/`smoke.sh` already present (endpoints exposed; seeds via `POST /cif/api/customers`) — not re-created; no separate seed file needed. `docs/flashcards.md` already had Step 15 — untouched. Interview Q&As (7 + STAR seed) + Key Terms (21) appended to `interview-bank.md` / `glossary.md`. Verification Log preserved intact (gateway routing, client deserialize+timeout, §12.3 StripPrefix mutation, clean-room/chain). **Multiple of 5 → added `## Cumulative Review (steps 1–15)`** — a 15-question mixed quiz spanning Step 1 (git) → 4 (JVM) → 5/7 (Spring/AOP) → 8–12 (data/concurrency/transactions) → 13–15 (MVC/idempotency/gateway), answers in `<details>`. Nothing unrecoverable. |
| 16 | ✅ enriched | Phase-C: Spring Security deep I — the **`auth` service** (filter chain, JWT, BCrypt). Build rewritten from 5 thin/fragment sub-steps to **10 full-micro-anatomy sub-steps** (0 auth module pom + root-pom diff · 1 `application.yml` + `AuthApplication` · 2 `SecurityConfig` **whole file** — the stateless filter chain + `PasswordEncoder`/`JwtEncoder`/`JwtDecoder` beans + claims→authorities converter · 3 `JwtService` whole file · 4 `UserService` in-memory BCrypt store whole file · 5 `AuthDtos` whole file · 6 `AuthController` whole file (/login, /me, /admin) + live curl flow · 7 `AuthSecurityTest` whole file (real-HTTP authn/authz) · 8 `PasswordEncodingTest` whole file + run all 9 · 9 play/verify harness — requests.http + smoke.sh + §12.3 mutation + ADR-0008) — **all code pasted verbatim from `step-16-end`** (HEAD has since grown the auth pkg in Step 17 — JWKS/asymmetric signing + method security; the tag is the truth). **1,652 lines / 116 fenced-delimiter lines** (was 652/42). Ran `./mvnw -pl services/auth test` **live** today → green on **Boot 4.0.6 / Java 25.0.3 / Tomcat 11.0.21** (random port 58369). **Test-count drift flagged per §12.8:** `step-16-end` `AuthSecurityTest` has **7** tests → **9** total; re-running at `main` shows **11** because Step 17 added `jwksEndpointPublishesPublicKeyOnly` + `methodSecurity_adminMethod_enforcesRole` (folded into Run-and-See §8, Verification Log §1/§7, and the troubleshooting table). `requests.http`/`smoke.sh` already present (endpoints exposed; demo users `alice`/`admin` seeded in-memory at startup) — not re-created; no separate seed file needed. `docs/flashcards.md` already had Step 16 — untouched. Interview Q&As (7 + STAR seed) + Key Terms (28) appended to `interview-bank.md` / `glossary.md`. Verification Log preserved intact (9 tests, live login→token→401/403/200, §12.3 `hasRole`→`permitAll` mutation 403→200, BCrypt, clean-room) + added §7 re-run-today/drift note. Not a multiple of 5 → no Cumulative Review. Nothing unrecoverable. |
| 17 | ✅ enriched | Phase-C: Spring Security deep II — **resource servers, RS256/JWKS, method security & modern auth**. Build rewritten from 5 fragment/abbreviated sub-steps to **12 full-micro-anatomy sub-steps** (1 auth `JwtService` HS256→RS256 diff+whole-file · 2 auth `SecurityConfig` RSA key + JWK source/encoder/decoder + `publicJwkSet` + `@EnableMethodSecurity` diff+whole-file · 3 auth `JwksController` new whole-file · 4 auth `AuthController` `@PreAuthorize /admin-method` diff+whole-file · 5 auth `AuthSecurityTest` +jwks/+method-security whole-file → 11 · 6 demand-account pom diff (security + oauth2-resource-server + security-test) · 7 `application.yml` `jwk-set-uri` diff+whole-file · 8 demand-account `SecurityConfig` new whole-file (resource server + roles converter + `@EnableWebSecurity`) · 9 `TransferController` `@PreAuthorize /api/v1/admin/ping` diff · 10 `TransferControllerTest` `jwt()` post-processor + `@MockitoBean JwtDecoder` whole-file · 11 `DemandAccountIntegrationTest` test-key `@TestConfiguration` + minted RS256 tokens whole-file → 31 · 12 requests.http + smoke.sh + ADR-0009) — **all code pasted verbatim from `step-17-end`** (HEAD has since refactored demand-account's web pkg heavily — payments/webhooks/security; the tag is the truth). **2,265 lines / 122 fenced-delimiter lines** (was 639/~40) — exceeds exemplar step-08 (1,985/124) and step-16 (1,652/116). Ran the **pure-JVM, unchanged-since-tag** `services/auth` module **live today** → real fresh output (11 tests = 9 `AuthSecurityTest` + 2 `PasswordEncodingTest`, Boot 4 / **Java 25.0.3**, random Tomcat port 64077, BUILD SUCCESS) folded into Run-and-See §5 + Verification Log §7; also a **fresh live JWKS fetch** (`kid 3d938754-…`) + login whose JWT header decoded to `{"kid":"3d938754-…","alg":"RS256"}` (kid matches JWKS) folded into sub-step 2 Run-and-See + Play §5. **Test-count drift flagged per §12.8:** demand-account at `HEAD` reports **>31** tests + a refactored web pkg (Steps 20–24 added Saga/outbox/batch/payments) — the recorded **31** is the `step-17-end` truth, used throughout; the demand-account `verify` Run-and-See blocks are marked verify-adjacent (cited from Verification Log §1, not re-run at HEAD). `requests.http`/`smoke.sh` already present (endpoints exposed; demo users `alice`/`admin` seeded in auth) — not re-created; no separate seed file needed. `docs/flashcards.md` already had Step 17 — untouched. Interview Q&As (7 + STAR seed) + Key Terms (22) appended to `interview-bank.md` / `glossary.md`. Verification Log preserved intact (11+31, live cross-service JWKS 401/201/403, §12.3 mutation, clean-room) + added §7 re-run-today/drift note. Not a multiple of 5 → no Cumulative Review. Nothing unrecoverable. |
| 18 | ✅ enriched | Phase-C finale: **secure coding & threat modeling** (STRIDE, OWASP×2, edge hardening). Build rewritten from 5 thin/fragment sub-steps to **11 full-micro-anatomy sub-steps** (1 threat-model header+§1 assets+§2 DFD/trust-boundaries · 2 §3 STRIDE-per-element ×6 · 3 §4 attack trees · 4 §5–6 OWASP Top 10 + API Top 10 · 5 §7 capstone + §8 secure-defaults checklist · 6 risk-register whole file · 7 SecurityConfig headers diff · 8 deny-by-default CORS bean + yml diff + **whole file** · 9 `SecurityHardeningTest` whole file + §12.3 mutation as break-it · 10 `SqlInjectionSafetyTest` whole file · 11 ADR-0010 + requests.http + smoke.sh + Makefile play-18 + sequence diagram) — **all artifacts pasted verbatim from `step-18-end` and diff-verified byte-identical** against the tag (threat-model fragments reassemble to the exact 281-line file incl. §-separators; preserved the tag's "Authentependent" typo, flagged in-line). **2,030 lines / 146 fenced-delimiter lines** (was 493/~40); exceeds exemplar step-08 (1,985/124). **Ran live today (2026-06-11):** the three Step-18 code files are unchanged since the tag (drift-checked), so re-ran the `play-18` command fresh → SqlInjectionSafetyTest 3/3 (Testcontainers **PostgreSQL 17.10**, random port 49845, Java 25.0.3, 10.49s) + SecurityHardeningTest 3/3 (4.594s) → BUILD SUCCESS 20.251s; also re-ran `smoke.sh` live → **PASSED**; fresh output folded into Run-and-See §9/§10/§11 + new Verification Log §6 (with §12.8 drift note: cif-24/da-34 module totals are tag truth; HEAD modules have grown via Steps 19–24). Mutation/headers/totals evidence preserved intact from the original Log §1–5. `requests.http`/`smoke.sh` already present (endpoints exposed; BOLA demo request D) — not re-created; no seed file needed (accounts opened via `POST /api/accounts`). `docs/flashcards.md` already had Step 18 — untouched. Interview Q&As (7 + STAR seed) + Key Terms (27) appended to `interview-bank.md` / `glossary.md`. Not a multiple of 5 → no Cumulative Review. Note: pre-existing uncommitted edits to `run-enrich.ps1`/`run-overnight-fixed.ps1` (model opus→fable, user's loop config) left untouched and uncommitted. Nothing unrecoverable. |
| 19 | ✅ enriched | Phase-D opener: **distributed-systems theory labs** (CAP/PACELC, consistency, quorums, clocks, delivery) — the `playground/distributed-lab` module. Build rewritten from 5 thin sub-steps to **11 full-micro-anatomy sub-steps** (1 scaffold — root-pom diff + module pom · 2 `LamportClock` · 3 `VectorClock` · 4 `LogicalClockTest` (3) · 5 `QuorumSystem` (brute-force `W+R>N` checker) · 6 `QuorumTest` (3, theorem over all 25 (W,R) pairs) · 7 `DeliverySim` · 8 `DeliverySemanticsTest` (4) · 9 `ReplicatedRegister` (CP/AP/LWW/heal) · 10 `CapPacelcTest` (3) + §12.3 mutation as break-it · 11 smoke.sh + Makefile `play-19` diff + full 13-test run + AP-partition sequence diagram) — **all 10 module files pasted complete & verbatim from `step-19-end`** (drift-checked: `git diff step-19-end..HEAD -- playground/distributed-lab steps/step-19` is empty, so tag == HEAD here). **1,936 lines / 134 fenced-delimiter lines** (was 372/~30). **Ran live today (2026-06-11), pure JVM, no Docker:** `clean test` → 13/13 BUILD SUCCESS 3.466s (Maven 3.9.12 / Java 25.0.3); per-class runs (3/3/4/3); `smoke.sh` → PASSED; plus **two real `jshell` sessions** against `target/classes` (Lamport 1/2/1/3 · vector `bob{bob=2, alice=1}` + concurrency · quorum true/false · naive 300 vs idempotent 100 · CP `Unavailable` · AP 200/300→300/300) folded into per-sub-step Run-and-See, 🎮 Play, and new Verification Log §6 (re-run + drift note). Original Verification Log §1–5 + §12.8 honesty note preserved intact (incl. the recorded `CapPacelcTest:25` mutation failure — not re-run, code frozen). **No `requests.http`/seed — pure-JVM JUnit lab, no endpoints/services** (step-11 precedent). Fixed a dangling promise: the stretch goal claimed `solutions/step-19/` which doesn't exist (only `solutions/step-01` does) → replaced with in-lesson design-hints `<details>`. `docs/flashcards.md` already had Step 19 — untouched. Interview Q&As (7 + STAR seed) + Key Terms (22) appended to `interview-bank.md` / `glossary.md`. Not a multiple of 5 → no Cumulative Review. Pre-existing uncommitted edits to `run-enrich.ps1`/`run-overnight-fixed.ps1` left untouched and uncommitted. Nothing unrecoverable. |
| 20 | ✅ enriched | Phase D: **event-driven** — Spring events + `@TransactionalEventListener`, transactional **Outbox**, Kafka (Redpanda), idempotent consumer, SSE; NEW `services/notification`. Build rewritten from 4 thin/fragment sub-steps to **15 full-micro-anatomy sub-steps** (1 producer wiring — da pom diff + `spring.kafka` yml diff · 2 `TransferCompletedEvent` · 3 `TransferEventListener` AFTER_COMMIT · 4 `V3__outbox.sql` (partial index) + `OutboxEvent` entity · 5 `OutboxEventRepository` (`Limit`) + `OutboxWriter` (Jackson-2, in-tx, no `@Transactional` — joins caller's) · 6 `TransferService.post()` diff (publish + outbox in ONE tx) · 7 `OutboxRelay` (send().get() → mark-after-ack; 3 crash windows narrated) · 8 `OutboxRelayScheduler` + `bank.*` yml diff + test `application.properties` (merge-not-shadow trap) · 9 `OutboxWriteTest` (NOT-@Transactional rationale) · 10 da `RedpandaContainers` + `OutboxRelayKafkaTest` (raw verification consumer) · 11 notification scaffold — root-pom diff + pom + app + yml (group-id/earliest) · 12 `Notification` + `SseHub` (COW vs synchronized contrast) · 13 `TransferEventConsumer` (Jackson-3 inject; `Set.add` gate) · 14 `NotificationController` + slice test + `TransferEventConsumerKafkaTest` capstone (3 received → 2 applied) · 15 Makefile diff + smoke.sh + ADR-0011 + sequence diagram) — **all code pasted verbatim from `step-20-end`** (HEAD has since hexagonal-refactored notification and grown da with payments/batch; the tag is the truth). **3,249 lines / 184 fenced-delimiter lines** (was 379/~25). **Ran live today (2026-06-11):** producer-side classes drift-checked byte-identical to tag → re-ran `OutboxWriteTest`+`OutboxRelayKafkaTest` fresh at HEAD → 3/3 BUILD SUCCESS 32.692s (Testcontainers Postgres 17.10, JDBC :63390, live AFTER_COMMIT listener line with fresh UUIDs) → folded into sub-steps 3/9/10 + new Verification Log §6 with §12.8 drift note (HEAD applies 4 migrations + Redis/Batch beans; tag-time 37+3 totals remain the truth; notification tag classes not re-runnable at HEAD; mutation not re-run — code frozen). Original Verification Log §1–5 preserved intact (37+3 tests, §12.3 dedupe mutation `expected: 2 but was: 3`, smoke dedupe log, clean-room 11 modules). `requests.http`/`smoke.sh` already present (endpoints exposed; accounts seeded via `POST /api/accounts` — no separate seed file needed) — not re-created. Fixed dangling promise: stretch goal referenced non-existent `solutions/step-20/` → replaced with in-lesson design-hints `<details>` (step-19 precedent). `docs/flashcards.md` already had Step 20 — untouched. Interview Q&As (7 + STAR seed) + Key Terms (25) appended to `interview-bank.md` / `glossary.md`. **Multiple of 5 → added `## 🧠 Cumulative Review (steps 1–20)`** — 16-question mixed quiz Step 1 (git) → 2 (records) → 4/11 (JVM/concurrency) → 5/7 (proxy rules) → 8/9/10 (Boot-4 modularization, partial index) → 12 (rollback scope) → 13 (async MVC) → 14 (two idempotency layers) → 15 (StripPrefix) → 16/17 (JWKS) → 18 (STRIDE on notification's no-auth) → 19→20 (delivery impossibility → the dedupe line) + an end-to-end eventId trace, answers in `<details>`. Nothing unrecoverable. |
| 21 | ✅ enriched | Phase D: **payments — Saga (orchestration) + compensation, Redis Idempotency-Key, Kafka retries + DLQ**. Build rewritten from 5 thin/fragment sub-steps to **14 full-micro-anatomy sub-steps** (0 Redis wiring — pom diff + `spring.data.redis` yml diff · 1 `PaymentFailedException` · 2 `PaymentStepService` (debit/credit/refund, each REQUIRES_NEW + Step-12 row lock) · 3 `RedisIdempotencyStore` (SET NX EX, 24h TTL) · 4 `PaymentService` orchestrator (NOT @Transactional; compensate-then-throw; 3-timeline ASCII) · 5 web layer — `PaymentRequest`/`PaymentResponse`/`PaymentController` (optional `Idempotency-Key` header) · 6 `GlobalExceptionHandler` 422 diff + **whole file** · 7 `RedisContainers` (`@ServiceConnection(name="redis")` on `GenericContainer` — the name-matching trap) · 8 `PaymentSagaTest` whole file + §12.3 mutation as break-it (recorded `expected: 100.00 but was: 60.0000`) · 9 `PaymentControllerTest` whole file (jwt() post-processor, key-forwarding verify) · 10 notification producer-serializer yml diff + `KafkaErrorHandlingConfig` (DefaultErrorHandler + DLT recoverer, FixedBackOff(0,2)=3 attempts) · 11 `TransferEventConsumer` stop-swallowing diff + method excerpt · 12 `DeadLetterTest` whole file + `TransferEventConsumerKafkaTest` **delta-assertion fix** diff (cached-context gotcha narrated) · 13 harness — Makefile play-21 diff + smoke.sh + ADR-0012 + two closing sequence diagrams) — **all code pasted verbatim from `step-21-end` and diff-verified byte-identical** against the tag (13 whole files + smoke.sh + 2 diffs' +/- lines). **2,802 lines / 162 fenced-delimiter lines** (was 349/~30). **Ran live today (2026-06-11):** demand-account payment pkg + both test classes are byte-identical to tag at HEAD → re-ran `PaymentSagaTest`+`PaymentControllerTest` fresh on real Testcontainers Postgres+Redis → 5/5 BUILD SUCCESS 21.329s (JDBC :65285, Boot 4.0.6/Java 25.0.3, genuine compensation WARN `payment 7131b6d9-… failed at credit … refund to ACC-A`) → folded into sub-steps 8/9 + new Verification Log §5 with §12.8 drift notes (HEAD applies 4 migrations — V4 is Step 24's; notification hexagonal-refactored at HEAD so its tag classes/totals 42+4 cited from recorded Log §1, not re-run; mutation not re-run — code frozen). Also ran `dependency:tree` fresh (starter 4.0.6 / Lettuce 6.8.2) for sub-step 0. Original Verification Log §1–4 + honesty note preserved intact. `requests.http`/`smoke.sh` already present (endpoints exposed; accounts seeded via `POST /api/accounts`) — not re-created. Fixed dangling promise: stretch goal referenced non-existent `solutions/step-21/` → replaced with in-lesson design-hints `<details>` (step-19/20 precedent). `docs/flashcards.md` already had Step 21 — untouched. Interview Q&As (7 + STAR seed) + Key Terms (25) appended to `interview-bank.md` / `glossary.md`. Not a multiple of 5 → no Cumulative Review. Pre-existing uncommitted edits to `run-enrich.ps1`/`run-overnight-fixed.ps1` left untouched and uncommitted. Nothing unrecoverable. |
| 22 | ⬜ pending | |
| 23 | ⬜ pending | |
| 24 | ⬜ pending | |
| 25 | ⬜ pending | multiple of 5 → ensure Cumulative Review (steps 1-25) |
| 26 | ⬜ pending | |
| 27 | ⬜ pending | |
| 28 | ⬜ pending | |
| 29 | ⬜ pending | |
| 30 | ⬜ pending | multiple of 5 → ensure Cumulative Review (steps 1-30) |

**Next step to enrich: 22**

When every step 11–30 is marked ✅, create an empty `STOP` file in the repo root and halt.
