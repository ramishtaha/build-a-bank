# VERIFICATION-LEDGER.md — per-tag verification history

> Moved out of `PROGRESS.md` on 2026-07-02 so the resume block stays small; new rows are appended here after each step per master prompt §15.3.

## Verification ledger (most recent first)
| Tag | Tier | `./mvnw verify` | Proof |
|---|---|---|---|
| `step-33-end` | 🟠 Standard | full-repo `./mvnw verify` → BUILD SUCCESS (14 modules, 04:18; auth 17 · gateway 7 · demand-account 48 unchanged — **zero Java changes**) + all-container topology live-verified + 🎓 **full-stack capstone 2/2 vs ZERO host services** + smoke.sh **6 gates PASSED** | **PHASE G OPENER — containerize everything.** ONE parameterized `deploy/Dockerfile.service` (ARG MODULE/PORT): temurin-25-jdk-alpine build with `--mount=type=cache,target=/root/.m2,sharing=locked` (Maven local repo has no locking; compose builds 7 images concurrently) → `jarmode=tools extract --layers --launcher` (4 layers; code-edit rebuild 9.3 s with deps CACHED, no-change 3.3 s) → `gcr.io/distroless/java25-debian13:nonroot` (uid 65532; `docker exec sh` → “executable file not found”; digests in VERSIONS). Image evolution measured: naive 471 MB → multistage 359 → layered 360 → distroless 360 → buildpacks 684 (works on Java 25, Liberica+SBOM) → **Jib 3.4.5 verified INCOMPATIBLE** (`class file major version 69` — PITest-style ASM lag; VERSIONS watch-item). JVM-vs-cgroup lab: MaxHeapSize 128 MiB (25% of 512m) / 384 MiB (75%) / 2.9 GiB (no limit!); `-m 64m` → OOMKilled=true ExitCode=137 (logs end mid-startup, no stack trace); `JAVA_TOOL_OPTIONS=-XX:MaxRAMPercentage=75.0` baked into images + `mem_limit: 512m` per service. JDK-25 AOT-cache lab (JEP 483/514, host): auth 3.762 s → 2.607 s (~30%); NOT baked into fleet — training runs need live infra (Flyway at build time would fail); ADR-0024. Compose: profile `bank` = 11 containers (7 services + spa + pg/redis/redpanda), initdb 2nd DB `cif` (psql \l shows both), redpanda DUAL listeners (internal redpanda:29092 / external localhost:9092 — rpk shows broker `redpanda 29092`); gateway env-only rewire via Step-15 `${services.*.uri}` placeholders (`SERVICES_DEMAND_ACCOUNT_URI` relaxed binding). Live wires: login 201-account read-back `balance:250.0000`, transfer → notification container log `notified: Transfer of 25.0…`, market-info rates via Redis, SPA `<div id="root">` through catch-all. Discovery: `/actuator/gateway` does NOT exist on WebMVC gateway → falls through to SPA (HTML 200) — step-15 lesson claim logged to IMPROVEMENT-BACKLOG. §12.3: no new Java code — no new mutation drill (prior drills stand). Nothing verify-adjacent: everything ran here (Docker 29.5.3). Makefile bank-up/-down/-ps/-logs + image-service. ADR-0024. |
| `step-32-end` | 🔴 Full | full-repo `./mvnw verify` → BUILD SUCCESS (14 modules; auth **17** tests incl. RefreshFlow 5 + RotationGrace 1, gateway **7**, demand-account **48**) + SPA: split build (login path **100.3 kB gzip**, was 127.8) / lint (0 errors, 1 pre-existing warning) / **29 Vitest** / hermetic E2E **2/2** / 🎓 **full-stack capstone E2E 2/2 against the REAL stack** (nginx SPA container → gateway :8080 → auth/ledger/Kafka/SSE, zero mocks) / smoke.sh **6 gates PASSED** / clean-room clone verify | **Frontend hardening & ship (Phase-F finale 🎖️).** Session: 10-min access JWT in-memory + rotating opaque refresh token (SHA-256-at-rest, CAS consume, sealed RotationResult) in httpOnly `bab_refresh` cookie (SameSite=Strict, Path=/api/auth, 12 h); reuse → family revocation; 3 s race grace → 409 + client single-retry; silent-refresh bootstrap + 3-state guard; localStorage debt RETIRED (risk C-10). §12.3 mutation BOTH sides: reuse-detection block deleted → exact test FAILED → revert green; client 401-retry deleted → 3 session tests FAILED by name → revert green. Wire transcript: replay-after-grace 401 + successor 401; replay-in-grace 409 + successor 200. Ship: multi-stage Dockerfile (node:22.20.0-alpine → nginx:1.28.3-alpine, digests in VERSIONS), nginx immutable/no-cache/try_files, gateway `Path=/**` catch-all LAST (order = list position; regression-tested) + credentialed CORS + wildcard-origin startup guard, `deploy/compose.fullstack.yaml` (pg 5433/redis/redpanda single-listener/spa). Capstone CAUGHT 2 real bugs: demand-account `GET /api/accounts/{id}` returned `currency:null` since ~Step 13 (MSW masked it; fixed + regression test) and proxied-CORS 403 (browser `Origin` on same-origin POST forwarded to :8082 → deny-by-default; fixed via `APP_CORS_ALLOWED_ORIGINS=http://localhost:8080`). SSE through servlet gateway verified live (same transactionId POST→SSE). Bundle: lazy routes + react-vendor manualChunks + visualizer 7.0.1 (`dist/stats.html`). ADR-0023. §12.8: real CDN/cloud deploy of the SPA container not runnable here (container+gateway topology is the local equivalent; Phase J covers cloud IaC). |
| `step-31-end` | 🟠 Standard | full-repo `./mvnw verify` → BUILD SUCCESS (14 modules) + SPA npm build (133 modules) / lint (0 errors, 1 benign warning) / **24 Vitest tests** (10 files) / **Playwright E2E 2/2** in real Chromium | **Frontend testing, a11y & i18n.** **MSW 2.14.6** network-level mocking (`src/mocks/{handlers,server}.ts`; `onUnhandledRequest:'error'`) replaces per-test `vi.mock` — LoginPage/TransferForm/AccountPanel specs now traverse real fetch. **a11y**: axe checks (`a11y.test.tsx`), labeled account selector, `role=alert/status`. **i18n**: react-i18next (en/es, synchronous resources) + `LanguageSwitcher` (EN→ES test, 317ms) + locale reset in `afterEach`. **Multi-currency**: `Intl.NumberFormat` (`i18n/format.ts`, `utils/currency.ts`). **Playwright 1.60** (browsers verified installed): `webServer` boots Vite with `VITE_API_BASE_URL=''` (same-origin) + `page.route` gateway mocks → login→balance→transfer→**live SSE notification** spec + wrong-password spec; `Idempotency-Key` asserted at the network layer. Gotchas: Vitest picked up the e2e spec (fixed via `test.exclude`), lint `no-explicit-any` in handlers (typed). §12.3 mutation: dropped `Idempotency-Key` from `client.transfer` → E2E FAILED (1 failed/1 passed), reverted → 2/2. smoke.sh PASSED. ADR-0022. Deps pinned (msw 2.14.6, i18next 24.2.3/react-i18next 15.7.4, @playwright/test 1.60.0, axe-core 4.12.0). §12.8: E2E is hermetic by design (route-mocked gateway); the live full-stack browser flow lands in Step 32's capstone. |
| `step-30-end` | 🟠 Standard | BUILD SUCCESS · full repo (14 modules, 3:14; gates green) + the SPA's npm build/lint/test | **Frontend data/forms/live.** `frontend/` + **TanStack Query** (`useAccount`/`useEntries` queries + `useTransfer` mutation → invalidates `['account']`/`['entries']`), `AccountPanel` (loading/error/data). **TransferForm** = **RHF + Zod** (`z.coerce.number().positive()`, fresh UUID Idempotency-Key). **SSE** `useNotificationStream` (EventSource → `transfer`) + `LiveNotifications`. API client + `getAccount`/`listEntries`/`transfer` (`/bank`, Bearer, ProblemDetail parsed). `npm run build` (tsc+vite, 111 modules) + lint (1 benign warning) + **15 Vitest tests** green (API + EventSource mocked). **Gateway** + `/notifications/**` route → notification:8084; `GatewayRoutingTest` **5 tests**. §12.3: dropped Zod `to` required → TransferForm test FAILED, reverted. smoke.sh PASSED. Clean-room `npm ci` green. ADR-0021. Deps pinned (react-query 5.101/RHF 7.78/zod 3.25.76/resolvers 3.10). §12.8: live browser flow (SSE-through-gateway buffering; demand-account CORS) verify-adjacent. |
| `step-29-end` | 🔴 Full | BUILD SUCCESS · full repo (14 modules, 2:59; gates green) + the SPA's own npm build/lint/test | **PHASE F OPENER.** New `frontend/` React 19 + TS + **Vite 6** SPA (npm, committed `package-lock.json`). `npm run build` (tsc+vite, 46 modules) + `npm run lint` (eslint 9; 1 benign react-refresh warning, exit 0) + `npm test` → **7 tests** (client 3, LoginPage 2, ProtectedRoute 2; API mocked). Typed API client (base URL = gateway), **AuthContext** (JWT login/logout, localStorage-persisted), **ProtectedRoute** guard, LoginPage/DashboardPage, BrowserRouter. **Gateway** = single front door: new `auth` route `/api/auth/**` (no StripPrefix) + `GatewayCorsConfig` `CorsFilter` (deny-by-default); `GatewayRoutingTest` → **4 tests** (route+strip, auth no-strip, CORS allow→ACA-O, CORS deny→403). §12.3: disabled the `ProtectedRoute` guard → redirect test FAILED (rendered protected content), reverted. jsdom gotcha: in-memory localStorage shim in `src/test/setup.ts`. smoke.sh PASSED. Clean-room `npm ci` green. ADR-0020. §12.8: live browser flow verify-adjacent (no browser) — headless CORS preflight verified; JWT-in-localStorage hardened Step 32. |
| `step-28-end` | 🔴 Full | BUILD SUCCESS · full repo (**14** modules, 3:02; quality gates on every module) | **🎓 Phase-E capstone + testing/quality mastery + custom starter.** **PITest 1.25.4** `-Pmutation` on the hexagon core (`Notification`+`NotificationService`): **5 mutations, 5 killed = 100%**, line 17/17 (NOT 1.19.1 — its ASM fails on JDK-25 `major version 69`). Fast core tests: `NotificationServiceTest` (Mockito, 2) + `NotificationTest` (1) + **jqwik** `NotificationPropertyTest` (1, 1000 cases). **Starter** `libs/common`=`common-spring-boot-starter` (`MoneyFormatter` `@AutoConfiguration`+`AutoConfiguration.imports`+`@ConditionalOnMissingBean`): `MoneyAutoConfigurationTest` (ApplicationContextRunner, 4) + `MoneyFormatterTest` (3); **consumed by `hello`** (`@SpringBootTest` injects the bean) + Boot-4 **`MockMvcTester`** slice (`HelloMockMvcTesterTest`). **Gates:** Spotless 3.6.0 (`lineEndings=PRESERVE`) + Checkstyle 13.5.0 (lean, **0 violations** ×14) bound to `verify`. **Error Prone 2.49.0 + NullAway 0.13.6 verified on JDK 25** (`-Perrorprone`, `:WARN`; planted `@Nullable` deref flagged). §12.3: dropped a `publish()` assertion → mutant SURVIVED → 80% < 90% → BUILD FAILURE, reverted → 100%. smoke.sh PASSED. Clean-room green. ADR-0019. |
| `step-27-end` | 🟠 Standard | BUILD SUCCESS · full repo (13 modules); notification **11** + demand-account **47** tests | **Architecture as fitness functions** (no new module; additive tests + test-scoped deps). **ArchUnit** (`HexagonalArchitectureTest`, no Docker): 4 rules enforce the Step-26 hexagon — `layeredArchitecture` (Domain/Application/Adapter, Adapter as ONE ring → web→push SSE allowed, adapter→core-inward forbidden), `domain_is_framework_free`, `application_does_not_depend_on_adapters`, `application_is_transport_agnostic`. **Spring Modulith** (`ModularityTest`, test scope, no Docker): `ApplicationModules.of(DemandAccountApplication.class)` → **9 modules**, `verify()` passes (DAG: web→{domain,payment,service,webhook}, service→{domain,event,outbox}, outbox→event, payment→domain, batch→domain; client isolated), `Documenter` → components.puml + 9 module canvases. §12.3 mutation ×2: `@Component` on domain → ArchUnit `domain_is_framework_free` FAILS; `event→outbox` ref → Modulith `Cycle detected: event -> outbox` FAILS; both reverted. smoke.sh PASSED. ADR-0018. |
| `step-26-end` | 🟠 Standard | BUILD SUCCESS · full repo (13 modules); notification **7** tests | **Hexagonal restructure** of `notification` (ports-and-adapters). Layers: `domain` (TransferEvent/Notification — only `java.*` imports), `application` (NotificationService use case + `port/in/NotifyOnTransfer` + `port/out/{ProcessedEventStore,NotificationPublisher}`), `adapter/in/{messaging,web}`, `adapter/out/{persistence,push}`. Dependency rule = inward. DDD tactical: value objects + application service, no aggregates (thin context). **Behaviour preserved** — the 4 integration + 2 unit tests pass with assertions UNCHANGED (imports-only moved). §12.3 mutation (use case ignores `markIfNew`) → `TransferEventConsumerKafkaTest…:67 expected: 2 but was: 3`, reverted. smoke.sh PASSED. ADR-0017. Sets up Step-27 ArchUnit enforcement. |
| `step-25-end` | 🟠 Standard | BUILD SUCCESS · full repo (13 modules); notification **7** tests | **PHASE E OPENER.** SOLID refactor of `notification`'s `TransferEventConsumer` (god-method → thin orchestration). **SRP**: extracted `TransferEvent`, `TransferEventParser` (Jackson isolated; poison→throws→DLT), `Notification.from(event)`. **DIP**: `ProcessedEventStore` **port** + `InMemoryProcessedEventStore` **adapter** (Redis-ready). **Behaviour preserved** — the 4 integration tests (`TransferEventConsumerKafkaTest`, `DeadLetterTest`, `NotificationControllerTest`) UNCHANGED & green (the safety net) + 2 new unit tests. §12.3 mutation (`markIfNew` always true) → `InMemoryProcessedEventStoreTest…:19 expected false but was true`, reverted. smoke.sh PASSED. ADR-0016. Standard tier → clean-room skipped (no critical path). |
| `step-24-end` | 🔴 Full | BUILD SUCCESS · full repo (13 modules); demand-account **44** tests | **END OF PHASE D 🎖️.** **Spring Batch** EOD interest-accrual job in demand-account (`InterestAccrualJobTest`, real Postgres): chunk read→process→write, COMPLETED; counts read 4 / write 2 / **filter 1** (zero balance) / **processSkip 1** (sentinel); ACC-1 1000→1000.10, ACC-2 500→500.05. JobRepository schema via Flyway **V4**; `spring.batch.job.enabled=false`, `initialize-schema=never`. **🎓 Phase-D capstone** (`PaymentExactlyOnceCapstoneTest`, real Postgres+Redpanda): idempotent transfer (1 movement) → Outbox→Kafka → forced duplicate → applied **exactly once** (scoped to the payment's txId). §12.3 mutation (remove batch `.skip`) → job `[FAILED]`, `…:73 expected: COMPLETED but was: FAILED`, reverted. smoke.sh PASSED. ADR-0015. Batch-6 packages: `infrastructure.item.*`, `core.job`/`core.step`. |
| `step-23-end` | 🔴 Full | BUILD SUCCESS · full repo (**13** modules); onboarding **4** tests (+ cif deactivate test) | **Onboarding orchestration** in a new `services/onboarding` (no DB). `OnboardingOrchestrationTest` (real HTTP, in-process `StubDownstream`, no Docker): happy path creates CIF customer → opens demand account, **bearer token forwarded** downstream; forced account-open failure → orchestrator **compensates** (`deactivateCalls==[42]`) + `OnboardingFailedException`(→502). `OnboardingControllerTest` (@WebMvcTest): 201 + 400. Declarative `@HttpExchange` clients (Step-15 pattern) + timeouts. cif: new `POST /api/customers/{id}/deactivate` (KYC→REJECTED) = the compensation target. §12.3 mutation (remove `cif.deactivate`) → `accountOpenFails…:89 Expecting [] to contain [42]`, reverted. smoke.sh PASSED. ADR-0014. Orchestration vs Step-21 choreography/Saga. |
| `step-22-end` | 🔴 Full | BUILD SUCCESS · full repo (**12** modules); market-info **5** tests | **Caching/async/clustered scheduling** in a new `services/market-info` (no DB), reusing Step-21 Redis. `MarketCacheTest` (real Redis): `@Cacheable` read model — a repeat read is served from cache (upstream called once per pair, `await`ed for write-visibility); `@CachePut` refresh overwrites; `@Async` warm runs on a **virtual thread**. `ShedLockTest` (real Redis): a held ShedLock lock blocks a 2nd acquire (cluster guard), re-acquirable after release. `MarketControllerTest` (standalone MockMvc). Redis `redis:7.4-alpine` via `@ServiceConnection(name="redis")`; ShedLock `6.10.0`. §12.3 mutation (remove `@Cacheable`) → `repeatReads…:45 ConditionTimeout … but was: 21`, reverted. smoke.sh PASSED. ADR-0013. Gotchas: networked-cache write-visibility (await, don't assume instant read-after-write); thin controller via standalone MockMvc; virtual threads scoped to @Async. |
| `step-21-end` | 🔴 Full | BUILD SUCCESS · full repo (11 modules); demand-account **42** + notification **4** tests | **Payments = Saga + Redis idempotency + DLQ.** demand-account: `PaymentService` orchestrates `PaymentStepService` (debit/credit/refund, each REQUIRES_NEW) with **compensation** (`PaymentSagaTest`: happy path moves money & writes 2 ledger legs; credit to a missing account → debit then **refund** → source restored, ledger nets 0; same `Idempotency-Key` → pays once, same paymentId — on real Postgres + Redis). `PaymentControllerTest`: `/api/v1/payments` forwards the key, unauth→401. Redis via Testcontainers `@ServiceConnection(name="redis")` (`redis:7.4-alpine`). notification: `DeadLetterTest` (real Redpanda) — poison message → `transfers.completed.DLT`, good message still processed. §12.3 mutation (remove compensation refund) → `PaymentSagaTest…:75 expected:100.00 but was:60.0000`, reverted. smoke.sh PASSED. ADR-0012. Gotcha: shared cached Spring context across same-config `@SpringBootTest`s → assert deltas, not totals. |
| `step-20-end` | 🔴 Full | BUILD SUCCESS · full repo (**11** modules); demand-account **37** + notification **3** tests | Event-driven bank. **demand-account**: `@TransactionalEventListener(AFTER_COMMIT)` + transactional **Outbox** (`OutboxWriteTest`: committed transfer → 1 outbox row in-tx + listener fires once; overdraft rollback → 0 rows, listener doesn't fire) + **Outbox relay → Kafka** (`OutboxRelayKafkaTest` on real Redpanda: relay publishes, a test consumer reads the record carrying the txId, row marked published, 2nd run publishes 0 = at-least-once). **notification** (NEW, no DB): idempotent `@KafkaListener` (`TransferEventConsumerKafkaTest` real Redpanda: 3 delivered, 2 distinct eventIds → 2 applied = exactly-once effect) + SSE (`NotificationControllerTest`: recent list + async SSE stream). Redpanda `v24.2.7` via Testcontainers `@ServiceConnection`. §12.3 mutation (disable dedupe) → `expected: 2 but was: 3`, reverted. smoke.sh PASSED. ADR-0011. Boot-4 gotcha: `spring-boot-starter-kafka` (not bare `spring-kafka`) for `KafkaTemplate`. |
| `step-19-end` | 🔴 Full | BUILD SUCCESS · full repo (**10** modules); distributed-lab **13** tests | **PHASE D OPENER.** New `playground/distributed-lab` (pure JVM, no Docker) — distributed-systems THEORY proven deterministically. **Clocks**: Lamport respects causality but can't detect concurrency; vector clock detects happens-before AND concurrency. **Quorums**: `everyWriteAndReadQuorumIntersect` brute-forced over ALL subsets == `W+R>N` (N=5); strict (3,2,2) reads latest, sloppy (3,1,1) reads stale. **Delivery**: naive ×3 → 300 (overcount) vs idempotent ×3 → 100 (exactly-once effect); 0× → loss. **CAP/PACELC**: CP partition → `Unavailable` thrown, replicas agree; AP partition → divergent reads (200/300) → `heal()` → both 300 (LWW); PACELC async → peer stale until `sync()`. §12.3 mutation (drop CP guard) → `CapPacelcTest…:25` FAILURE, reverted. smoke.sh PASSED. No new ADR (Step-11 lab idiom). |
| `step-18-end` | 🔴 Full | BUILD SUCCESS · full repo (9 modules); cif **24** + demand-account **34** tests | **END OF PHASE C.** STRIDE threat model (`security/threat-model.md`: DFD + 5 trust boundaries + 2 attack trees + OWASP Top 10 / API Top 10 walkthrough + capstone) & risk register (`security/risk-register.md`). **BOLA (R-001, CRITICAL)** found on transfer/balance/entries → recorded & scheduled, not half-fixed (§12.8 honesty). Hardening: explicit headers (nosniff/DENY/no-referrer/HSTS) + **deny-by-default CORS**. `SqlInjectionSafetyTest` (parameterized matches **0** vs concat contrast matches **1**; destructive payload → table survives). `SecurityHardeningTest` (headers present; un-listed-origin preflight → **403** no-ACAO; unauth → 401). §12.3 mutation (CORS `["*"]`) → `crossOriginPreflight…:73 expected:<403> but was:<200>`, reverted. smoke.sh PASSED. ADR-0010. Gotcha: CORS source must resolve by NAME (`.cors(Customizer.withDefaults())`), not by-type injection (2 beans). |
| `step-17-end` | 🔴 Full | BUILD SUCCESS · full repo (9 modules); auth **11** + demand-account **31** tests | auth RS256 + JWKS (`/oauth2/jwks` public-only: `kty:RSA`, no `d/p/q`) + `@PreAuthorize` (403/200). demand-account = OAuth2 resource server (`jwk-set-uri`), money endpoints authenticated, `@PreAuthorize` admin. **Live cross-service**: auth JWKS → demand-account no-token 401, auth-token 201, USER admin-ping 403. Slice via `jwt()` post-processor (+`@EnableWebSecurity`+mock decoder); integration via test JwtDecoder + minted RS256 (no committed keys). §12.3 mutation (anyRequest permitAll) → `expected:<401> but was:<200>`, reverted; `steps/step-17/smoke.sh` PASSED. ADR-0009. |
| `step-16-end` | 🔴 Full | BUILD SUCCESS · full repo (**9** modules); auth **9** tests | New `services/auth` (Spring Security 7, in-memory users, no Docker). Live JWT flow: login alice→HS256 JWT (sub/roles/exp, 1800s); wrong pw→401; /me no token→401; /me→`{"username":"alice","roles":["ROLE_USER"]}`; /admin as USER→403, as ADMIN→200; `X-Content-Type-Options: nosniff`. BCrypt (`$2…`, salted, matches). Filter chain: `SecurityFilterChain` lambda DSL, stateless, CSRF off, oauth2ResourceServer jwt; Nimbus encoder/decoder; BCryptPasswordEncoder. §12.3 mutation (admin `permitAll`) → `expected 403 but was 200`, reverted; `steps/step-16/smoke.sh` PASSED. ADR-0008. SS7 `FACTOR_BEARER` filtered from /me. |
| `step-15-end` | 🔴 Full | BUILD SUCCESS · full repo (**8** modules); gateway **1** + demand-account **27** tests | New `gateway/` (Spring Cloud Gateway Server WebMVC, servlet). `GatewayRoutingTest`: forwards to in-test stub, `StripPrefix` (`/cif/api/customers/1`→`/api/customers/1`), `AddResponseHeader X-Gateway`. `spring-cloud-starter-gateway-server-webmvc` resolves+boots on Boot 4.0.6/SC 2025.1.1 (prefix `spring.cloud.gateway.server.webmvc.routes`). `CifClient` (`@HttpExchange` over `RestClient` via `HttpServiceProxyFactory`, JDK connect+read timeouts): `CifClientTest` deserialize + read-timeout→`ResourceAccessException`. §12.3 mutation (remove StripPrefix) → `expected "/api/customers/1" but was "/cif/api/customers/1"`, reverted; `steps/step-15/smoke.sh` PASSED. ADR-0007. Resilience4j deferred to Step 37. |
| `step-14-end` | 🔴 Full | BUILD SUCCESS · full repo (7 modules); demand-account **25** tests | `/api/v1` namespace. **Idempotency** (IdempotencyTest): same key → same txId, balance 150 (once); diff key → 100; no key → 180. **Webhook** (WebhookSignerTest 4 + WebhookDeliveryTest 2): HMAC sign/verify; tamper/wrong-secret/stale-timestamp rejected; in-test receiver verified our signature; transient-500 → retried (calls≥2). **Pagination** (integration, real HTTP): `PageResponse` {content,totalElements,size:2}. **Deprecation**: old `/api/transfers` → `Deprecation: true`. §12.3 mutation (remove replay window) → `aStaleTimestampIsRejected` `false but was true`, reverted; `steps/step-14/smoke.sh` PASSED. ADR-0006. No new deps. Boot-4 Jackson-3 gotcha worked around. |
| `step-13-end` | 🔴 Full | BUILD SUCCESS · full repo (7 modules); demand-account **13** tests | RFC 9457 ProblemDetail: live overdraft → 422 `application/problem+json` {type,title:"Insufficient funds",status:422,detail,instance}; validation → 400 + `errors.amount`. springdoc 3.0.3 (Boot-4): live `/v3/api-docs` → OpenAPI 3.1 with title+paths; `/swagger-ui/index.html` → 200. RequestIdFilter → live `X-Request-Id` header; TimingInterceptor → `X-Timing-Enabled: true` (both on a real transfer 200). §12.3 mutation (status 422→200) → `expected:<422> but was:<200>`, reverted; `steps/step-13/smoke.sh` PASSED. springdoc pinned in VERSIONS.md. |
| `step-12-end` | 🔴 Full | BUILD SUCCESS · full repo (7 modules); demand-account **11** tests | NEW `services/demand-account` (own Postgres). Double-entry transfer: A=150/B=50, balanced ledger pair, conserved, ledgerNet 0; overdraw → InsufficientFundsException + full rollback (0 ledger rows). REQUIRES_NEW audit survives outer rollback. Optimistic `@Version` → `ObjectOptimisticLockingFailureException` (balance 210, version 1). 🎓 **Capstone**: no-lock lost update (A=100,B=100 via CyclicBarrier); pessimistic `FOR UPDATE` 20 concurrent → A=0/B=1000, total 1000, ledgerNet 0. Live HTTP (RANDOM_PORT): 201/201/200/422. §12.3 mutation (remove `FOR UPDATE`) → 17/20 fail (`expected 0 but was 17`), reverted; `steps/step-12/smoke.sh` PASSED. ADR-0005. |
| `step-11-end` | 🔴 Full | BUILD SUCCESS · full repo (6 modules); concurrency-lab **8** tests | New `playground/concurrency-lab` (pure JVM). Lost update **deterministic** (CyclicBarrier → balance=1 not 2); `UnsafeBalance` 8×100k lost ~609k of 800k; `synchronized`/`AtomicLong`/`LongAdder` exact at 800k. Virtual threads 10k/10k; `Semaphore(3)` capped at 3. §12.3 mutation (remove `synchronized`) → `expected 800000 but was 145225`, reverted; `steps/step-11/smoke.sh` PASSED. JCStress verify-adjacent (§12.8). |
| `step-10-end` | 🔴 Full | BUILD SUCCESS · cif **21** tests (+11 raw-JDBC labs) | Real Postgres 17.10 via Testcontainers 2.0.5 (random port e.g. 49575). Plans: Seq Scan (`Rows Removed by Filter: 19996`) → Bitmap Index Scan → Index Only Scan (`Heap Fetches: 0`). Isolation: no dirty read; non-repeatable @RC (100→200); stable @RR (100→100); phantom @RC (2→3) not @RR (2→2). **Write skew**: −100 @REPEATABLE READ vs rejected @SERIALIZABLE (SQLSTATE 40001, sum held 50). HikariCP size-2 pool: 3rd borrow timed out ~506 ms, recovered on return. Partition pruning: only `txn_2026_02` scanned. Online DDL: CIC→25001 in txn, builds in autocommit; fast default backfills 1000 rows. §12.3 mutation (SERIALIZABLE→REPEATABLE READ) → "Expecting code to raise a throwable" at `WriteSkewTest.java:89`, reverted; `steps/step-10/smoke.sh` PASSED. ADR-0004. |
| `step-09-end` | 🔴 Full | BUILD SUCCESS · 50 tests (+4 cif) | N+1 proven via Hibernate statistics (3 stmts lazy vs 1 with `@EntityGraph`); `@Version` optimistic-lock conflict → `ObjectOptimisticLockingFailureException`; interface projection; §12.3 mutation (remove `@Version`) caught; `steps/step-09/smoke.sh` PASSED |
| `step-08-end` | 🔴 Full | BUILD SUCCESS · 46 tests (+6 cif) | CIF on real Postgres (Testcontainers, random port 57881, PG 17.10); Flyway v1 migrated; live POST→201/GET→200, 400, 404; §12.3 mutation (404→200) caught + reverted; `steps/step-08/smoke.sh` PASSED |
| `step-07-end` | 🟠 Standard | BUILD SUCCESS · 40 tests (+6 spring-lab) | `@Around` audit aspect fires on `@Audited` HTTP calls; `AccountService$$SpringCGLIB$$0` proxy; self-invocation pitfall proven by counter; capstone slice 200/404; `steps/step-07/smoke.sh` PASSED. **Phase A complete.** |
| `step-06-end` | 🟠 Standard | BUILD SUCCESS · 34 tests (+4 spring-lab) | typed `@ConfigurationProperties` binding; custom `GreetingAutoConfiguration` (on/off/back-off); `/actuator/conditions` 141 applied / 82 skipped on hello-service; `steps/step-06/smoke.sh` PASSED |
| `step-05-end` | 🟠 Standard | BUILD SUCCESS · 28 tests (+6 spring-lab) | conditional beans (fixed/market via `@ConditionalOnProperty`), constructor DI, singleton vs prototype scopes, SpEL, lifecycle order 1→4 + `@PreDestroy` in app run; `steps/step-05/smoke.sh` PASSED |
| `step-04-end` | 🟠 Standard | BUILD SUCCESS · 22 tests (+2 jvm) | `javap -c` bytecode; `-Xlog:gc` G1 young pauses; `-XX:+PrintCompilation` C1/C2/OSR; JFR summary; `-Xlog:class+load` (CDS); escape-analysis discovery; `steps/step-04/smoke.sh` PASSED |
| `step-03-end` | 🟠 Standard | BUILD SUCCESS · 20 tests (+4 net) | `LoopbackHttpTest` (JDK HttpServer) round-trips via `HttpClient` + raw socket; `HttpClientDemo`/`RawHttpDemo` vs hello-service; curl -v / nslookup / TLS captures; `steps/step-03/smoke.sh` PASSED |
| `step-02-end` | 🟠 Standard | BUILD SUCCESS · 18 tests (java-basics 16 + hello 2) | `Step2Demo` prints net 1124.50 USD; `steps/step-02/smoke.sh` PASSED; records/sealed/streams/Optional/java.time exercised |
| `step-01-end` | 🟠 Standard | BUILD SUCCESS · 2/2 tests | Tomcat 11.0.21, random test port, `GET /api/hello` → 200 JSON, `/actuator/health` → UP, repackaged jar |
| `step-01-start` | 🟢 Light | BUILD SUCCESS | parent aggregator, no modules yet |

---

*The sections below were also moved verbatim from `PROGRESS.md` on 2026-07-02 (they no longer fit the slim resume block; nothing was dropped).*

## Done so far
- ✅ **Step 0 — capability preflight** → `CAPABILITIES.md` (JDK 25.0.3 LTS, Maven 3.9.12, Docker running, no local k8s, scanners install-on-demand).
- ✅ **Version set resolved & verified to build** → `VERSIONS.md` + `adr/0002` (Java 25 + Spring Boot 4.0.6 + Spring Cloud 2025.1.1; Spring AI flagged for re-pin at Phase I).
- ✅ **Repo scaffold** — parent POM, Maven Wrapper, Makefile, `.gitignore`/`.env.example`/`.editorconfig`/`.tool-versions`, ADR-0001/0002, README, COURSE.md.
- ✅ **Step 1** — `steps/step-01/lesson.md` (full §8 contract), `requests.http`, `smoke.sh`, and the working `services/hello` app. Tagged `step-01-start` (scaffold) and `step-01-end` (verified green).
- ✅ **Steps 2–13** — all tagged `step-NN-end` and green (see the Verification ledger below). **Phase A complete** (Steps 1–7). **Phase B complete** (Steps 8–12): CIF + JPA/Flyway/Testcontainers, Hibernate perf, relational DB up close, concurrency/thread-safety, demand-account ledger + locking. **Phase C ✅ complete** (Steps 13–18): Step 13 (Spring MVC deep — ProblemDetail, OpenAPI/Swagger, filters/interceptors), Step 14 (API design — versioning/deprecation, idempotency, pagination, signed webhooks) on demand-account, Step 15 (API Gateway/BFF + service-to-service HTTP), Step 16 (Spring Security deep I — filter chain, JWT, BCrypt; new auth service), Step 17 (Spring Security deep II — RS256/JWKS, demand-account as resource server, method security), Step 18 (secure coding & threat modeling — STRIDE threat model + risk register, OWASP Top 10 / API Top 10 walkthrough, edge hardening headers+CORS, injection-safety test; BOLA/R-001 found & tracked). **Phase D ✅ complete** (Steps 19–24): Step 19 (distributed-systems theory — CAP/PACELC, consistency, quorums, logical/vector clocks, delivery semantics — proven in a pure-JVM `distributed-lab`), Step 20 (event-driven: Spring events + `@TransactionalEventListener`, transactional Outbox, Kafka/Redpanda producer+consumer, idempotent consumer = exactly-once effect, SSE real-time push; new `services/notification`), Step 21 (payments: orchestration **Saga** + compensation, **Redis** Idempotency-Key, Kafka retries + **DLQ**), Step 22 (caching & async: `@Cacheable` Redis read model / CQRS read side, `@Async` on virtual threads, `@Scheduled` + **ShedLock** clustered scheduling; new `services/market-info`), Step 23 (retail onboarding **orchestration** across cif + demand-account via `@HttpExchange` clients, with compensation + token forwarding; new `services/onboarding`), Step 24 (**Spring Batch** EOD interest-accrual chunk job with skip/retry; JobRepository via Flyway V4; **🎓 Phase-D capstone** = exactly-once effect end-to-end). **End of Phase D 🎖️.** **Phase E in progress** (Steps 25–28): Step 25 (SOLID refactor of `notification` — SRP extractions + a DIP `ProcessedEventStore` port/adapter; behaviour-preserving), Step 26 (full **hexagonal** restructure of `notification` — domain/application/adapter layers, inbound+outbound ports, DDD value objects; behaviour-preserving), Step 27 (**enforcing architecture** = fitness functions: **ArchUnit** enforces the notification hexagon's dependency rule; **Spring Modulith** verifies demand-account's 9 modules are cycle-free + generates living docs; both proven to fail on an injected violation), Step 28 (**testing & quality mastery** + a custom starter: **PITest** mutation testing to **100%** on the hexagon core = 🎓 **Phase-E capstone**; **jqwik** property tests; `libs/common` → a real **auto-configured Spring Boot starter** consumed by `hello`; Boot-4 **MockMvcTester**; **Spotless + Checkstyle** quality gates in `verify`; Error Prone/NullAway verified on JDK 25). **End of Phase E 🎖️.** **Phase F in progress** (Steps 29–32): Step 29 (**frontend foundations** — a new React 19 + TypeScript + **Vite** SPA in `frontend/`: typed API client → the gateway, **AuthContext** JWT login flow, **ProtectedRoute** guard, react-router; Vitest + Testing Library tests; the **gateway** extended to front `auth` + deny-by-default CORS), Step 30 (**state, data & forms** — **TanStack Query** account/ledger queries + transfer mutation with cache invalidation; **React Hook Form + Zod** validated transfer form with Idempotency-Key; **SSE** live notifications via `EventSource`; gateway extended to front the notification stream; 15 Vitest tests). Modules so far: `libs/common` (starter), `services/{hello, cif, demand-account, auth, notification, market-info, onboarding}`, `gateway`, `playground/{java-basics, spring-lab, concurrency-lab, distributed-lab}`, **`frontend/`** (React+TS+Vite SPA — npm, not Maven).

## Known watch-items (carried forward)
- **Spring AI** is RC on the Boot-4 line → re-pin to GA at Phase I (Step 46+), or use the Python FastAPI sidecar path.
- **ErrorProne/NullAway** ✅ RESOLVED (Step 28): 2.49.0/0.13.6 **verified working on JDK 25** (off-by-default `-Perrorprone` profile, `:WARN`). Spotless + Checkstyle are the always-on gates in `verify`.
- **Node/npm for the frontend** ✅ RESOLVED (Step 29): Node 22.20.0 / npm 11.16.0 present, npm registry reachable → the SPA builds/lints/tests here. The one verify-adjacent gap is a **live browser** cross-origin flow (no browser in the sandbox) — verified via headless CORS preflight + Vitest/Testing-Library instead (§12.8).
- **Frontend JWT storage** (Step 29): the JWT is in `localStorage` (XSS-exposed) — a teaching simplification. Harden in **Step 32** (httpOnly cookie / in-memory + refresh-token rotation + route-guard hardening).
- **Kubernetes/cloud** are verify-adjacent in this sandbox (no local cluster) → learner installs `kind`; we lint/template/dry-run.

## Pinned facts
See `VERSIONS.md` (versions) and `CAPABILITIES.md` (what runs here). Never `latest`. Money = BigDecimal; time = UTC/Instant.
